# AppForge Studio — Fix de 8 Bugs Criticos
> Fecha: 2026-05-05 | Version: v0.9 | Previo a CI build

## Resumen
Corregidos 8 bugs criticos identificados en code review externo (v0.8) en 6 archivos fuente.

## Bugs corregidos

| Bug | Archivo | Cambio |
|-----|---------|--------|
| BUG1 | SatinRenderer.swift | Padding `_pad1`/`_pad2` en GPUPBRMaterial, GPUPointLight, GPUDirectionalLight |
| BUG2 | SatinRenderer.swift | `updateAnimation()` eliminado de `render(in:)` (solo queda en Coordinator.draw) |
| BUG3 | SatinRenderer.swift | `[UInt16]`->`[UInt32]`, `UInt16()`->`UInt32()`, `.uint16`->`.uint32` |
| BUG4 | Package.swift | `mattrajca/Satin` -> `s1ddok/Satin` |
| BUG5 | Shaders.metal, PBRShaders.metal | Normal matrix: `transpose(inverse(float3x3(modelMatrix)))` |
| BUG6 | Shaders.metal | Eliminado `aspect` de stroke (no hardcodear aspect como center.w) |
| BUG7 | SculptEngine.swift | `dragDelta: SIMD3<Float>` en SculptPoint para grab deformer |
| BUG8 | CanvasViewModel.swift | `@Published var currentMode: AppMode = .hybrid` (antes hardcodeado) |

## Estado post-fix
- Structs GPU con padding 16-byte align para Metal
- Animacion llamada una vez por frame
- Index buffers compatibles con mallas >65535 vertices
- Dependencia Satin apunta a s1ddok (no mattrajca)
- Normal matrix matematicamente correcta
- Stroke sin distorsion de aspect ratio
- Grab deformer funcional
- Modo seleccionable desde UI

## Proximos pasos (en orden)
1. Push a GitHub -> build-ios.yml en macos-14/Xcode 16.1
2. Ejecutar 23 tests en CI
3. Validar render loop (paint+sculpt+CAD+animation) en simulador iOS 17+
4. Generar .ipa sin firma para AltStore
5. Beta TestFlight v0.9
