# Issue tracker

Este repo usa **GitHub Issues** (`iwannatrip02-sys/AppForge-Studio`) vía el CLI `gh`.

Las skills de ingeniería (`to-tickets`, `to-spec`, `triage`, `qa`) crean y leen
issues con `gh issue create` / `gh issue list`.

PRs como superficie de pedidos: **off**.
